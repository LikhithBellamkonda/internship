{
  "entities": {
    "Settings": {
      "title": "Global Settings",
      "description": "Global configuration for the internship portal.",
      "type": "object",
      "properties": {
        "requirements": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of internship requirements."
        },
        "formUrl": {
          "type": "string",
          "format": "uri",
          "description": "The URL of the Google Form for applications."
        },
        "ownerEmail": {
          "type": "string",
          "format": "email",
          "description": "The email of the portal owner."
        }
      },
      "required": ["requirements", "formUrl"]
    }
  },
  "firestore": {
    "/internship_settings/global": {
      "schema": "Settings",
      "description": "Single document containing global portal settings."
    }
  }
}
